let input = ['flower', 'flow', 'flight'];
let temp = ['a', 'ab', 'abc', 'b']
let temp2 = []

let longestCommenPrefix = function(arr){
    if(arr.length === 0) return '';
    let arrSorted = arr.sort((a,b) =>  b.length - a.length)
    
}

longestCommenPrefix(temp2)